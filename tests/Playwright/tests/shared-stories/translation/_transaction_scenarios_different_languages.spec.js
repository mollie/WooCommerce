const { expect } = require('@playwright/test');
const { test } = require('../../Shared/base-test');

test.describe('_Transaction scenarios - Different languages', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

test.skip('[C5594] Validate translations', async ({ page}) => {
  // Your code here...
});


});
