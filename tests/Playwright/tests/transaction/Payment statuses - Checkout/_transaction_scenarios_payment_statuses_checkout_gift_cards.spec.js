const { expect } = require('@playwright/test');
const { test } = require('../../Shared/base-test');

test.describe('_Transaction scenarios_Payment statuses Checkout - Gift cards', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });


// TestId-C3438
    test.skip('[C3438] Validate the submission of an order with any payment method including a Gift Card and marked as "Paid"', async ({ page }) => {
        // Your code here...
    });

// TestId-C3439
    test.skip('[C3439] Validate the submission of an order where the total value is paid with a Gift Card', async ({ page }) => {
        // Your code here...
    });


});
