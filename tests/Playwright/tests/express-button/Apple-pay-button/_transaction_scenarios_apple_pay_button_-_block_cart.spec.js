const { expect } = require('@playwright/test');
const { test } = require('../../../fixtures/base-test');

test.describe('_Transaction scenarios - Apple Pay button - Block Cart', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });


test.skip('[C420208] Validate that Apple Pay button is displayed per UI design in block cart', async ({ page}) => {
  // Your code here...
});


test.skip('[C420209] Validate that Apple Pay button is hidden when usupported product type is in the block cart', async ({ page}) => {
  // Your code here...
});


test.skip('[C420210] Validate the submission of an order with Apple Pay button as payment method and payment mark as "Paid" from block cart', async ({ page}) => {
  // Your code here...
});


});
