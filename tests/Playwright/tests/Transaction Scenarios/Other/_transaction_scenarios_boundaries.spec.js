const { expect } = require('@playwright/test');
const { test } = require('../../Shared/base-test');

test.describe('_Transaction scenarios - Boundaries', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

test.skip('[C3359] Validate the order minimum amount restrictions for all payment methods', async ({ page}) => {
  // Your code here...
});


test.skip('[C3360] Validate the order maximum amount restrictions for all payment methods', async ({ page}) => {
  // Your code here...
});


});
