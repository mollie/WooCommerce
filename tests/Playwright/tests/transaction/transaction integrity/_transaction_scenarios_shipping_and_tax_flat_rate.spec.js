const { expect } = require('@playwright/test');
const { test } = require('../../../fixtures/base-test');

test.describe('_Transaction scenarios_Shipping and tax - Flat rate', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });


//TestId-C420077
test.skip('Make a successful payment with random item and shipping flat rate using Order API', async ({ page}) => {
  // Your code here...
});


//TestId-C420078
test.skip('Make a successful payment with random item and shipping flat rate using Payment API', async ({ page}) => {
  // Your code here...
});


});
