const { expect } = require('@playwright/test');
const { test } = require('../Shared/base-test');

test.describe('_Transaction scenarios_Payment statuses - Block Checkout - Apple Pay', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

//TestId-C420217
test.skip('Validate the submission of an order with Apple Pay as payment method and payment mark as "Paid" on block checkout', async ({ page}) => {
  // Your code here...
});


});
