const { expect } = require('@playwright/test');
const { test } = require('../Shared/base-test');

test.describe('_Transaction scenarios_Shipping and tax - Including tax', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

//TestId-C420081
test.skip('Make a successful payment with random item including tax using Order API', async ({ page}) => {
  // Your code here...
});


//TestId-C420082
test.skip('Make a successful payment with random item including tax using Payment API', async ({ page}) => {
  // Your code here...
});


});
