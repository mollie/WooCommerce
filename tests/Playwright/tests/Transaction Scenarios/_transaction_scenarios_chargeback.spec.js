const { expect } = require('@playwright/test');
const { test } = require('../Shared/base-test');

test.describe('_Transaction scenarios - Chargeback', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

test.skip('[C3443] Validate the creation of a chargeback on an order paid with Credit Card', async ({ page}) => {
  // Your code here...
});


});
