const { expect } = require('@playwright/test');
const { test } = require('../../Shared/base-test');

test.describe('_Transaction scenarios_Payment statuses - Pay for order page - Apple Pay', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });
   

//TestId-C420333
test.skip('Validate the submission of an order with Apple Pay as payment method and payment mark as "Paid"  on pay for order page', async ({ page}) => {
  // Your code here...
});


});
