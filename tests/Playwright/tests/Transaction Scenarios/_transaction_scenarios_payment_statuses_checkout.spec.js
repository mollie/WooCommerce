const { expect } = require('@playwright/test');
const { test } = require('../Shared/base-test');

test.describe('_Transaction scenarios - Payment statuses Checkout', () => {
  test.beforeEach(async ({ page }) => {
    //code before each
  });

test.skip('[C3442] Validate selecting a new payment method after the first payment failed', async ({ page}) => {
  // Your code here...
});

test.skip('[C3344] Validate that the ecommerce admin can map a response from Mollie to a status in the ecommerce', async ({ page}) => {
    // Your code here...
});

});
